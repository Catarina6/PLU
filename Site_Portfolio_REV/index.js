function showVideo(videoId) {
    switch (videoId) {
        case 1:
            const dialogVideo = document.getElementById("varsovie")
            dialogVideo.showModal()
            break;

        default:
            break;
    }
}